"use client";

import { Template } from "src/components/template";

export default function Dashboard() {
    return (
        <Template>
            <div>Dashboard
            </div>
        </Template>
    );
}
